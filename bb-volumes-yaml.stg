/*
 * Mandatory:
 *
 * vars.volume.class
 * vars.volume.storage
 *
 */

/*
 * 
 */
bb-volumes-yaml(parent, vars) ::= <<
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: anrisoftware-com-data-1
  namespace: anrisoftware-com
  labels:
    group: anrisoftware-com
  annotations:
    volume.beta.kubernetes.io/storage-class: <vars.volume.class>
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: <vars.volume.storage>

>>
